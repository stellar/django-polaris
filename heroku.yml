build:
  docker:
    reference-server: Dockerfile
release:
  image: reference-server
  command:
    - poetry run python manage.py migrate
run:
  watch_transactions:
    image: reference-server
    command:
      - poetry run python manage.py watch_transactions
  process_pending_deposits:
    image: reference-server
    command:
      - poetry run python manage.py process_pending_deposits
  execute_outgoing_transactions:
    image: reference-server
    command:
      - poetry run python manage.py execute_outgoing_transactions --loop
  poll_outgoing_transactions:
    image: reference-server
    command:
      - poetry run python manage.py poll_outgoing_transactions --loop
  web:
    image: reference-server
    command:
      - poetry run gunicorn server.wsgi
