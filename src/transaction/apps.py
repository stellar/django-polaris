"""This module configures the transaction app."""
from django.apps import AppConfig


class TransactionConfig(AppConfig):
    """This stores metadata for the transaction app."""

    name = "transaction"
